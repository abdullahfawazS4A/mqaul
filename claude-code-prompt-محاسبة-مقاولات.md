# Prompt لـ Claude Code — موقع محاسبة مقاولات (Frontend فقط)

## الفكرة العامة
أريد بناء موقع ويب بسيط لمحاسبة أعمال مقاولات، **Frontend فقط بدون أي Backend أو Database**. البيانات تُدار بالكامل عبر state داخل التطبيق (React state)، بدون أي اتصال بسيرفر أو تخزين دائم في هذه المرحلة — سيتم ربط Backend لاحقًا.

## المتطلبات التقنية
- React (Vite) + Tailwind CSS.
- تصميم **بسيط جدًا وواضح**، بدون تعقيد بصري أو عناصر زائدة.
- Responsive بالكامل: يفتح بشكل سليم على الموبايل، التابلت، واللابتوب (استخدم mobile-first مع breakpoints واضحة).
- بنية ملفات نظيفة: مكوّن منفصل لكل واجهة/قسم.
- كل البيانات تكون Mock/Local State قابلة للتعديل بسهولة لاحقًا عند ربط API حقيقي (اجعل طبقة البيانات معزولة، مثلاً hooks أو context منفصل باسم واضح، لتسهيل الاستبدال بـ Backend لاحقًا).

## البنية العامة (Navigation)
شريط تنقل رئيسي (Sidebar على الشاشات الكبيرة، وقائمة/Bottom nav على الموبايل) يحتوي على قسمين رئيسيين:

1. **الحسابات** (فيها 3 واجهات فرعية: الصيرفة، الديون، القوائم)
2. **المشاريع**

---

## أولاً: واجهة الصيرفة (Treasury)
- تمثل حركة الإيداع والاستلام (Cash In / Cash Out) من الصندوق الرئيسي.
- عناصر:
  - رصيد الصيرفة الحالي (يُحسب تلقائيًا من مجموع العمليات).
  - زر "إضافة إيداع" وزر "إضافة استلام/سحب".
  - نموذج الإضافة: المبلغ، التاريخ، ملاحظة/سبب.
  - جدول/قائمة بالعمليات (النوع، المبلغ، التاريخ، الملاحظة)، مرتب من الأحدث للأقدم.

---

## ثانيًا: واجهة الديون (Debts)
- تدير الأشخاص الذين لهم دين (أموال مستحقة لهم من الصيرفة/رأس المال).
- عناصر:
  - رأس المال / الرصيد الكلي المتاح للديون (يُدخل يدويًا كإدخال أولي، ويمكن تعديله).
  - قائمة الأشخاص (المستخدمين)، مع إمكانية "إضافة مستخدم" جديد (اسم، رقم هاتف اختياري، ملاحظات).
  - لكل شخص: رصيد الدين الحالي الخاص به.
  - زر "إضافة دين" لشخص معيّن — **يجب أن يمنع النظام إضافة دين إذا كان المبلغ المطلوب أكبر من الرصيد الكلي المتاح** (Validation واضح مع رسالة خطأ).
  - زر "إضافة سند قبض" (تسديد جزء أو كل الدين من شخص معيّن) — يُعيد المبلغ إلى الرصيد الكلي.
  - سجل حركات لكل شخص (تاريخ، نوع العملية: دين/سند قبض، المبلغ، ملاحظة).

---

## ثالثًا: واجهة القوائم (Lists / Invoices)
- قائمة بسيطة **بدون أي عمليات حسابية مرتبطة بالصيرفة أو الديون** — هي فقط عرض/تسجيل شكلي.
- كل قائمة (سجل) تحتوي على الحقول التالية:
  - اسم الشخص
  - رقم القائمة
  - ملاحظات
  - قيمة القائمة (رقم فقط، **بدون أي تأثير حسابي** على باقي النظام — للعرض فقط)
  - ربح القائمة (رقم)
  - حالة الربح: **واصل (مقبوض)** أو **دين (غير مقبوض)** — Toggle أو Select بسيط.
- جدول يعرض كل القوائم مع إمكانية إضافة/تعديل/حذف قائمة.
- فلترة بسيطة اختيارية حسب الحالة (واصل / دين).

---

## رابعًا: واجهة المشاريع (Projects)
- قائمة بكل المشاريع + زر "إضافة مشروع جديد".
- بيانات المشروع الأساسية:
  - اسم المشروع
  - القيمة المالية للمشروع
  - الشركة المنفذة
  - الشركاء (قائمة أسماء، يمكن إضافة أكثر من شريك)
- داخل صفحة تفاصيل كل مشروع (عند الضغط عليه):
  - **إيداعات الشركاء**: سجل بمبلغ كل إيداع، اسم الشريك، التاريخ.
  - **المصاريف**: سجل بمبلغ كل مصروف، وصف/سبب المصروف، **من قام بالصرف** (اسم الشخص)، والتاريخ.
  - **السلف المستلمة**: سجل بمبلغ كل سلفة مستلمة، من جهة/شخص، والتاريخ.
  - ملخص علوي للمشروع: مجموع الإيداعات، مجموع المصاريف، مجموع السلف (عرض بسيط بالأرقام).

---

## ملاحظات تصميم عامة
- ألوان هادئة ومحايدة (لا تصميم صاخب)، خطوط واضحة، تباعد جيد بين العناصر.
- استخدم بطاقات (Cards) وجداول بسيطة بدل الجداول المعقدة.
- كل النماذج (Forms) تفتح كـ Modal أو صفحة بسيطة، بدون تعقيد.
- دعم اللغة العربية RTL في كامل الواجهة.
- لا حاجة لتسجيل دخول/مصادقة في هذه المرحلة (سيُضاف لاحقًا مع الـ Backend).

## المطلوب منك (Claude Code)
ابنِ المشروع كاملاً بالبنية والمواصفات أعلاه، مع تقسيم واضح للمكوّنات، وطبقة بيانات وهمية معزولة يسهل استبدالها بـ API حقيقي لاحقًا.
